/* This line of code is exporting the `sizeMapping` and `statusMapping` variables from a file named
"mapping". By using this syntax, other files can import these variables from the "mapping" file
using the same names. */
export { sizeMapping, statusMapping } from "./mapping";